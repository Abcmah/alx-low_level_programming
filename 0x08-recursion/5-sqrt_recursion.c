#include "main.h"
#include <math.h>
/**
 *_sqrt_recursion - Return squareroot
 *@n: holds a number to be sqted
 *Return: str
 */


int _sqrt_recursion(int n)
{
	if(n == 0 || n == 1)
	{
		return n;
	}
	for(i = 1; i*i <=n; ++i);

	re
}
